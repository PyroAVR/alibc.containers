/**
 * This file is a template for Meson to generate the real vcs_info.c with.
 * The file gets built into each of the shared objects, the symbol will then
 * be available during debugging and can be included in traces and debug logs.
 */
static const char ALC_CONTAINERS_COMMIT_HASH[] = "@VCS_TAG@";
